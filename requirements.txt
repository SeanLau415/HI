# ── VPS Worker Requirements ──────────────────────────────────────────────────
# Install: pip install -r requirements.txt

# HTTP with Cloudflare-bypass browser impersonation
curl_cffi>=0.6.3

# HTML parsing
beautifulsoup4>=4.12.0
lxml>=5.1.0          # faster BS4 parser

# RSS / Atom feed parsing
feedparser>=6.0.11

# Multi-platform notifications (Telegram, Email, Slack, Discord, etc.)
apprise>=1.7.6

# YAML config parsing
PyYAML>=6.0.1

# ── Optional but recommended ─────────────────────────────────────────────────
# aiofiles>=23.2.1     # async file I/O if extended
# python-dotenv>=1.0.0 # load .env for secrets
